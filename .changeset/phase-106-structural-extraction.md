---
"gitsema": minor
---

Add `gitsema index start --graph` to extract raw structural references (imports, calls, `extends`/`implements`) from TS/TSX/JS/Python blobs into a new `structural_refs` table (schema v25), laying the groundwork for the upcoming knowledge-graph traversal commands.
